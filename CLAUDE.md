# school-schedule

## Deployment

This project is published via GitHub Pages from the `main` branch.

**Always merge completed changes to `main` and push to trigger publication.**

After any feature branch work is done, merge to `main`:

```bash
git checkout main
git merge <branch>
git push origin main
```
